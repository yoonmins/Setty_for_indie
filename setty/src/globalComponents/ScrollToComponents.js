import React, { useRef } from "react";

const ScrollToComponents = () => {
//   // 각 섹션을 참조하기 위한 useRef
//   const section1Ref = useRef(null);
//   const section2Ref = useRef(null);
//   const section3Ref = useRef(null);

//   const scrollToSection = (ref) => {
//     if (ref.current) {
//       ref.current.scrollIntoView({ behavior: "smooth" }); // 부드럽게 스크롤 이동
//     }
//   };

  return (
    <div>
      {/* <div
        style={{
            position: 'fixed',
            bottom: '10px',
            left: '0',
            width: '100%',
            display: 'flex',
            justifyContent: 'center',
            gap: '10px',
            background: 'rgba(255, 255, 255, 0.8)',
            padding: '10px',
            boxShadow: '0 -2px 5px rgba(0,0,0,0.2)',
        }}>
        <button onClick={() => scrollToSection(section1Ref)}>Go to Section 1</button>
            <button onClick={() => scrollToSection(section2Ref)}>Go to Section 2</button>
            <button onClick={() => scrollToSection(section3Ref)}>Go to Section 3</button>
        </div> */}
    </div>    
  );
};

export default ScrollToComponents;